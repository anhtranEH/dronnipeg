<h1> Products </h1>
<div class="container">
  <h1> Home Page </h1>
  <div class="row">
    <% @products.each do |product| %>
      <div class="column">
        <%= render partial: "product", locals: { product: product } %>
      </div>
    <% end %>
  </div>
</div>
