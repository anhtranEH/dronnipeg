<h1> Products </h1>
<div class="container">
  <h1> Home Page </h1>
  <div class="row display-flex">
    <% @products.each do |product| %>
      <%= render partial: "product", locals: { product: product } %>
    <% end %>
  </div>
</div>
